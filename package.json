{
  "name": "react-boilerplate",
  "version": "0.1.0",
  "description": "A template for modern web applications",
  "author": "soulsbros",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "@soulsbros/eslint-config": "^1.0.7",
    "@soulsbros/prettier-config": "^1.0.3",
    "husky": "^7.0.4",
    "recursive-install": "^1.4.0"
  },
  "scripts": {
    "lint": "eslint ./ --ext .js --fix",
    "ri": "npm i && npm-recursive-install",
    "prepare": "husky install"
  }
}
